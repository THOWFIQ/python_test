Running SQL:
        SELECT TEMPLATE_ID, REGION, FORMAT_TYPE, USERNAME, USEREMAIL,
               FILTERS, TEMPLATENAME, WORKORDERID, SHARED,
               SHAREDUSERNAME, COLUMNS
        FROM TEMPLATE_DATA

With Bind Params: {}
❌ Failed to parse filters: the JSON object must be str, bytes or bytearray, not LOB
❌ Failed to parse columns: the JSON object must be str, bytes or bytearray, not LOB
❌ Failed to parse filters: the JSON object must be str, bytes or bytearray, not LOB
❌ Failed to parse columns: the JSON object must be str, bytes or bytearray, not LOB
